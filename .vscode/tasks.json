{
    "tasks": [
        {
            "type": "shell",
            "label": "deleteGCDA",
            "command": "find ${command:cmake.buildDirectory} -name '*.gcda' -type f -delete",
        }
    ]
}
